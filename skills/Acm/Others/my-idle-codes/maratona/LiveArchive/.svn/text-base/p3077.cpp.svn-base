#include <stdio.h>

int main() {
	int n;

	scanf("%d", &n);

	while (n--) {
		unsigned long long int need,have;
		scanf("%lld%lld", &have, &need);

		if (have>=need)
			printf("MMM BRAINS\n");
		else
			printf("NO BRAINS\n");		
	}

	return 0;
}
